-- ============================================================
-- 第十四階段:跨裝置儀表板偏好
-- 在 001 之後執行。可重複執行。
-- 把儀表板自訂存到 profiles(隨帳號跨裝置),沿用 003 的「本人可更新自己」政策。
-- ============================================================

alter table profiles add column if not exists dashboard_prefs jsonb not null default '{}'::jsonb;
